name: labeler
on:
  pull_request_target:
    types: [opened]

permissions:
  contents: read
  pull-requests: write

jobs:
  label:
    uses: ihabfallahy2/workflows-core/.github/workflows/labeler.yml@v1
    secrets: inherit
