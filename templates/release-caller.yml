name: release
on:
  push:
    branches: [master]

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  release:
    uses: ihabfallahy2/workflows-core/.github/workflows/release.yml@v1
    secrets: inherit
